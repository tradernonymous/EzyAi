# fly.toml app configuration file generated for ezyai on 2026-09-05T07:15:35Z
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = 'ezyai'
primary_region = 'ams'

[build]
  dockerfile = 'Dockerfile'

[env]
  EZYAI_LOG_LEVEL = 'INFO'
  EZYAI_STATE_FILE = '/data/state.json'
  PORT = '8080'

[[mounts]]
  source = 'ezyai_state'
  destination = '/data'
  # Fly keeps daily volume snapshots; two weeks covers a late-noticed
  # state problem. Restore with `fly volumes snapshots list/create`.
  snapshot_retention = 14

[http_service]
  internal_port = 8080
  auto_stop_machines = 'off'
  auto_start_machines = true

  [[http_service.checks]]
    grace_period = '30s'
    interval = '30s'
    timeout = '5s'
    method = 'GET'
    path = '/'

[[vm]]
  memory = '512mb'
  cpu_kind = 'shared'
  cpus = 1
