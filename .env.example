TELEGRAM_BOT_TOKEN=your_bot_token_from_botfather

# When true, market data falls back to a built-in synthetic generator
# whenever live API calls fail (useful for offline demos/tests).
EZYAI_DEMO_DATA=false

# Where watches / autopilot state is persisted.
EZYAI_STATE_FILE=state.json

# Logging level: DEBUG, INFO, WARNING, ERROR
EZYAI_LOG_LEVEL=INFO

# Operator contact sent in the User-Agent to SEC EDGAR (their fair-access
# policy requires one; requests without it are blocked).
EZYAI_CONTACT_EMAIL=

# GET / returns 503 when the background scheduler has not completed a tick
# for this many seconds, so the host restarts a wedged bot.
EZYAI_HEALTH_STALE_S=180

# ---- Monetization ----
# Native Stripe card checkout (dynamic prices, no preset Price IDs).
# Get both from dashboard.stripe.com (Developers -> API keys / Webhooks).
STRIPE_API_KEY=
STRIPE_WEBHOOK_SECRET=
# Bot username (no @) for Stripe success/cancel redirects.
BOT_USERNAME=ezytradeai_bot

# USDT (TRC-20) receiving address shown to manual payers.
USDT_ADDRESS=

# Telegram user id that approves/rejects USDT claims (also always-PRO).
ADMIN_TELEGRAM_ID=

# Comma-separated Telegram user ids with permanent PRO (team monitoring).
# Editable anytime via Fly secret, no redeploy needed.
PRO_ACCESS_IDS=
# ---- Website card checkout (printezy.money) ----
# PRO bought by card on the EzyMap website is delivered here: the bot pulls
# unclaimed purchases for a Telegram username from the site's entitlement
# API (on /start, /plans, /account, PRO gates and a 2-minute sweep).
# Must equal EZYAI_ENTITLEMENT_KEY on the website. Empty = feature off.
EZYAI_SITE_URL=https://printezy.money
EZYAI_SITE_KEY=
# Legacy: also claim purchases by Telegram username (handles can change
# owners). Set false once the website shows buyers a /redeem code.
EZYAI_SITE_USERNAME_MATCH=true

# Optional: Sentry DSN for error reporting (off when empty).
SENTRY_DSN=
